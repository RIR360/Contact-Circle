{
  "name": "contact-circle-api",
  "version": "1.0.0",
  "description": "api server for contact circle app",
  "main": "Contact-circle.js",
  "scripts": {
    "start": "pm2 start Contact-circle.js",
    "restart": "pm2 restart Contact-circle",
    "stop": "pm2 stop Contact-circle",
    "logs": "pm2 logs Contact-circle",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rejwan Islam Rizvy (RIR360)",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pm2": "^5.3.0"
  }
}
